-- 035: Контекст для разметки shadow-лога роутера.
-- Последняя реплика ассистента перед сообщением клиента — чтобы при ручной
-- разметке было видно: «SERA спросила X → клиент ответил Y → роутер выбрал Z».
-- NULL, если перед сообщением клиента не было реплики ассистента (начало диалога).

ALTER TABLE router_shadow_log
  ADD COLUMN IF NOT EXISTS last_assistant_message TEXT;
